---
import "./series-visual.css";
---

<figure class="agent-map series-visual not-prose">
  <div class="user">
    <strong>用户提出任务</strong><span>目标、约束与运行中的补充</span>
  </div>
  <div class="connection">
    ↓ 输入任务 <span aria-hidden="true">／</span> ↑ 显示进度与回答
  </div>
  <div class="harness">
    <p class="boundary">Coding Agent 的 harness 层</p>
    <div class="interface module">
      <strong>交互与任务生命周期</strong><span
        >接收输入、显示进度、处理停止与追加要求</span
      >
    </div>
    <div class="connection">
      ↓ 启动与控制 <span aria-hidden="true">／</span> ↑ 进度与结果
    </div>
    <div class="engine">
      <div class="module">
        <strong>模型接入</strong><span>转换不同模型服务的消息格式</span><small
          >对接外部模型服务</small
        >
      </div>
      <span class="exchange" aria-hidden="true">↔</span>
      <div class="module loop">
        <strong>Agent Loop</strong><small>Agent 循环</small><span
          >请求模型 → 执行工具</span
        ><small>记录结果，再决定是否继续</small>
      </div>
      <span class="exchange" aria-hidden="true">↔</span>
      <div class="module tools">
        <strong>工具系统</strong><span>读取、编辑、搜索、命令</span><small
          >操作文件与命令环境</small
        >
      </div>
    </div>
    <p class="exchange-description">
      Agent Loop 通过模型接入层获取回复，调用工具，并把执行结果交回模型。
    </p>
    <div class="support-group">
      <p class="group-label">配套模块 <span>参与运行的不同阶段</span></p>
      <dl class="support">
        <div>
          <dt>上下文管理</dt>
          <dd>选择发给模型的信息，处理历史过长时的压缩。</dd>
        </div>
        <div>
          <dt>会话管理</dt>
          <dd>保存消息历史，支持恢复任务与创建分支。</dd>
        </div>
        <div>
          <dt>权限与信任</dt>
          <dd>检查项目指令是否可信、工具操作是否被允许。</dd>
        </div>
        <div>
          <dt>扩展机制</dt>
          <dd>增加工具、命令，或在运行过程中执行自定义逻辑。</dd>
        </div>
      </dl>
    </div>
    <p class="child">
      <strong>子代理协作</strong>主 Agent 委派子任务，其他 Agent
      单独处理并返回结果。
    </p>
  </div>
  <figcaption>
    每个实线框表示一个模块，最外层框表示 harness
    层的范围。箭头表示调用与反馈；配套模块不按图中排列顺序执行。测试与评估覆盖所有模块。
  </figcaption>
</figure>

<style>
  .agent-map {
    container-type: inline-size;
    margin: 2rem 0;
    font-size: 0.9rem;
    line-height: 1.65;
    color: var(--series-ink);
  }
  .user {
    max-width: 19rem;
    margin: 0 auto;
    padding: 0.75rem 1rem;
    border: 1px dashed var(--series-secondary);
    border-radius: 0.5rem;
    background: var(--series-paper);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.15rem;
  }
  .user span,
  .module small,
  .interface span {
    color: var(--series-secondary);
    font-size: 0.8rem;
  }
  .connection {
    padding: 0.7rem 0;
    text-align: center;
    color: var(--series-secondary);
    font-size: 0.8rem;
  }
  .harness {
    border: 1px solid var(--series-secondary);
    border-top: 3px solid var(--series-blue);
    border-radius: 0.6rem;
    padding: 1.1rem;
    background: var(--series-canvas);
  }
  .boundary {
    margin: 0 0 1rem;
    color: var(--series-ink);
    font-size: 0.8rem;
    font-weight: 600;
  }
  .interface {
    display: flex;
    flex-direction: column;
    text-align: center;
  }
  .engine {
    display: grid;
    grid-template-columns: 1fr auto 1.25fr auto 1fr;
    align-items: stretch;
    gap: 0.5rem;
  }
  .module {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 0.35rem;
    padding: 0.9rem 0.6rem;
    border: 1px solid var(--series-secondary);
    border-radius: 0.5rem;
    background: var(--series-paper);
    text-align: center;
    min-width: 0;
  }
  .module strong {
    font-size: 0.95rem;
  }
  .module span {
    font-size: 0.8rem;
  }
  .loop {
    background: var(--series-blue-wash);
    border: 1px solid var(--series-blue);
    border-radius: 0.4rem;
  }
  .tools {
    border-color: var(--series-feedback);
  }
  .tools small {
    color: var(--series-feedback);
  }
  .exchange {
    align-self: center;
    color: var(--series-blue);
  }
  .exchange-description {
    margin: 0.9rem 0;
    font-size: 0.8rem;
    color: var(--series-secondary);
  }
  .support-group {
    margin-top: 1.25rem;
    border-top: 1px solid var(--series-secondary);
    padding-top: 1rem;
  }
  .group-label {
    margin: 0 0 0.7rem;
    font-size: 0.85rem;
    font-weight: 600;
  }
  .group-label span {
    display: inline-block;
    margin-left: 0.5rem;
    color: var(--series-secondary);
    font-size: 0.75rem;
    font-weight: 400;
  }
  .support {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: 0.75rem;
    margin: 0;
  }
  .support > div {
    padding: 0.8rem;
    border: 1px solid var(--series-secondary);
    border-radius: 0.5rem;
    background: var(--series-paper);
  }
  .support dt {
    font-weight: 600;
  }
  .support dd {
    margin: 0.3rem 0 0;
    font-size: 0.8rem;
    color: var(--series-secondary);
  }
  .child {
    margin: 1rem 0 0;
    border: 1px solid var(--series-secondary);
    border-radius: 0.5rem;
    background: var(--series-paper);
    padding: 0.8rem;
    font-size: 0.8rem;
  }
  .child strong {
    display: block;
    margin-bottom: 0.3rem;
    font-size: 0.9rem;
  }
  .agent-map figcaption {
    margin: 0.65rem 0 0;
    font-size: 0.8rem;
    color: var(--series-secondary);
  }
  @container (max-width: 540px) {
    .harness {
      padding: 1rem;
    }
    .engine {
      grid-template-columns: 1fr;
      gap: 0.2rem;
    }
    .module {
      padding: 0.6rem;
    }
    .exchange {
      justify-self: center;
      transform: rotate(90deg);
    }
    .support {
      grid-template-columns: 1fr;
      gap: 0.8rem;
    }
  }
</style>
