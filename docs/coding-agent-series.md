# Coding Agent 原理与实践：写作与展示约定

## 目标、读者与作者背景

本系列以原理教学为主：让有 Coding Agent 使用经验的读者理解各模块如何工作，以及实现时需要处理哪些问题。imp 源码作为解释原理的实例，不按提交历史记录开发流水账。

作者提供的背景：模型能力提升，harness 让模型参与实际工作；现有产品在使用门槛、过程可见性、设计与完成度上未满足自己的偏好。pi 的简洁和自定义能力吸引了作者，因此参考 pi，从零实现自己的编程助手，既学习 Agent 的原理，也希望用它解决真实问题。

写作时保留作者的观点与声音。对 Claude Code、Codex 和国产产品的评价明确属于个人使用体验，不扩写成普遍事实；显示模型已返回的信息，不等于能够取得模型未提供的内部推理。

## 组织原则

- 章节按理解与实现的依赖关系组织，以最小执行闭环为起点。
- 每章以“小场景 → 带注释的源码节选 → 异常处理”为主线，按需加入模拟运行，不以提交、里程碑或作者开发过程串起正文。模拟应帮助读者理解状态变化、执行时序或数据累积；若只是重复源码流程，可以省略。
- 00 交代背景、整体架构和目录；不放开发时间线、提交列表、当前功能清单或未来功能计划，项目现状由仓库文档维护。01 用源码节选解释循环；02、03 分别拆开模型接入与工具实现。
- 04 讨论本次请求携带什么；05 讨论历史如何保存和恢复，明确上下文与持久化的区别。
- 权限与测试从相关模块开始出现，07 和 10 专章系统整理。
- 不按“核心程度”硬排序，也不将提交日志直接拆成博客。

## 章节与展示清单

| 章  | 核心主题                         | imp 实现与历史素材                                                        | 首选展示                                            | 可增加的演示                               |
| --- | -------------------------------- | ------------------------------------------------------------------------- | --------------------------------------------------- | ------------------------------------------ |
| 00  | 为什么开发 imp：背景、架构与导读 | 作者动机、模块职责与任务流程                                              | 职责架构图、任务流程、系列目录          | 无需交互                                   |
| 01  | 最小 Agent 与执行循环            | `src/core/loop.ts`、`test/loop.test.ts`、`test/loop-hooks.test.ts`        | 最小伪代码 → 加入停止、校验、异常与中断；消息序列图 | 按步推进，联动伪代码与新增消息             |
| 02  | 模型接入与消息协议               | `src/provider/types.ts`、`resolve.ts`、各 provider 与协议测试             | 内部消息与请求 JSON 对照、流式时序图                | 流式分片逐步组装工具参数                   |
| 03  | 工具系统                         | `src/core/tools/types.ts`、`src/core/loop.ts`、`src/runner.ts`、调用与钩子测试 | 工具接口、注册的两条去向、统一调用与结果协议 | 无需独立模拟，源码与工具实例已覆盖调用流程 |
| 04  | 上下文管理                       | `system-prompt.ts`、`context-files.ts`、`compaction.ts`、相关测试         | 上下文组成图、估算与压缩伪代码、摘要前后对照        | 模拟窗口预算，查看实际保留内容；标明估算值 |
| 05 | 会话与持久化 | `src/core/session/`、`src/repl/replay.ts` | JSONL 关键记录示例、恢复源码、压缩记录与恢复历史对照图 | 无需独立交互演示 |
| 06  | 交互与任务生命周期               | `src/repl/`、`src/tui.ts`、中断与输入队列测试                             | 状态图、事件处理伪代码、短终端录屏                  | 播放一段脱敏终端记录，支持暂停与文字稿     |
| 07  | 权限与信任边界                   | `src/core/trust.ts`、`guardian.mjs`、信任与拦截测试                       | 项目加载与工具执行的两条检查路径、决策伪代码        | 本地规则模拟，查看允许、拒绝和确认原因     |
| 08  | 扩展机制                         | `src/extensions/`、`commands-md.ts`、M4 设计与案例                        | 扩展 API 表、注册与事件伪代码、故障隔离图           | 静态示例即可，按需增加钩子执行演示         |
| 09  | 子代理与并发协作                 | `subagent.ts`、`tools/task.ts`、`file-lock.ts`、`worktree.ts`             | 委派伪代码、上下文边界图、并发时序与 diff           | 对照共享目录和 worktree 的文件变化         |
| 10  | 测试与评估             | `test/helpers/fakes.ts`、回归测试、真实验收与审查记录                     | 测试/评估伪代码、失败用例、已核验结果表             | 有真实测量数据后，再添加带条件说明的图表   |

01–10 是模块主线。所有章节都可以根据内容量与理解难度拆成上下篇，写作前评估一次，成稿后再次检查。目录数字表示阅读顺序，不表示开发阶段；M0/M1 等历史编号只用于内部素材查证。

### 上下篇拆分原则

- 不以字数作为唯一标准。若一篇需要解释多个相对独立的机制、不断引入新概念，或需要多段大型源码和多个演示才能讲清，应评估拆分。
- 按知识依赖和完整问题划分，每篇都应有明确的问题、完整的解释过程和可总结的结论，不能把同一段核心流程或对应异常处理生硬截断。
- 模块章拆分后仍围绕场景、核心源码和相关异常展开，模拟按需添加；异常跟随所属机制，避免统一堆到下篇。上下篇之间只保留必要的回顾与过渡。
- 拆分时保留原章节编号，并用“上／下”和具体题目区分，明确阅读顺序，同步文章导航与篇间链接。
- 第 02 章初稿拆成上下篇：上篇“请求与消息转换”，下篇“流式响应与组装”。两篇分别保留场景、核心源码、模拟、异常与收尾；本地预览时上下篇互链，发布时需一并核对链接。

## 单章模板

1. 小场景：先给一个读者熟悉的任务，让模块有具体的使用背景。用自然语言说明任务如何推进，数据结构、协议字段和接口名留到源码部分，先交代用途再展示。
2. 源码与原理：先展示精简的核心源码，用中文注释标出执行步骤、重复关系和停止条件，再按步骤分节分析。开头提示读者先看整体流程，不必立即理解每个参数；数据结构在对应小节解释用途。保留 imp 源码的主要语句和命名，明确省略了什么，不把改写稿标成完整源码。局部只节选当前分析需要的代码，文末不再重复整段循环。核心代码保留计数、上限和中断的真实检查位置与停止原因，长分支可以明确省略并指向异常小节。异常处理一节对应这些位置解释处理原因；循环篇只解释工具错误怎样进入历史，不展开工具内部校验、权限检查或 try/catch 实现。中断后的结果补齐属于循环职责，需要保留。
3. 模拟运行（可选）：仅在逐步观察能带来额外理解时使用，联动执行位置、输入输出和状态；预设数据明确标注模拟。不为统一形式重复已有讲解。
4. 异常处理：讲清触发条件、处理方式和需要保留的状态，必要时继续展示源码。

文章保留系列编号；正文和子节按实际结构连续编号，如 `1.`、`2.1`，删去模拟或增加工具实例后同步调整目录与交叉引用。01 增加第 `5.` 节简短收尾，概括循环职责并衔接下一篇，避免在异常处理末尾重复引出下一篇；文章 01 文末不再渲染完整系列目录，章节导航由侧栏提供。00 作为导读按内容编号 `1.` 至 `6.`，架构子节用 `4.1` 至 `4.3`，不强套模块章的四步模板。直接从场景进入，不堆砌写作目标、抽象总结或反复强调的结论。用具体动作解释原理，少用“闭环”“边界”“关键是”等泛化措辞；不虚构作者经历。不在正文堆提交哈希、里程碑、测试数量与开发复盘。实现依据留在内部素材笔记，文章按需要链接源码。

### 源码位置注释

- 每个源码节选代码块开头用注释标明源文件的仓库相对路径，例如 `src/core/loop.ts`；文件链接保留在正文中。
- 函数内部的节选标明所在函数，必要时注明阶段、分支或嵌套回调；完整函数声明已包含名称时，无需重复标注。模块级类型或参数定义按实际位置说明，不虚构所属函数。
- 正文中不同文件、不同函数的源码节选分别使用独立代码块，用简短文字串起调用关系，不拼成一段连续代码。同一函数中相隔较远的片段，注明中间省略的步骤，或按阶段拆开展示。
- 不固定源码行号；源码调整后复核函数位置。模拟数据不标成源码，JSON 示例不插入不符合其语法的注释。

## 表达检查约定

- 开发动机集中说明一次，避免在开头、正文和总结反复列同样的目标。
- pi 等参考项目首次出现时先交代是什么；图示和目录也采用读者能理解的表述。
- 原理部分优先使用通用概念：完整助手称 Coding Agent（语境明确后简称 Agent），组织执行的软件层称 harness，具体职责用执行循环、模型接入层、工具系统等模块名。imp 只用于作者背景、源码来源和具体实现取舍，不用项目名代替通用原理。
- 介绍执行循环时保留输入准备和输出去向：用户消息在哪里写入、工具怎样查找、模型回答怎样展示。区分事件驱动的显示、完整消息的保存和函数返回的运行状态，不能把停止状态写成回答正文。循环篇用简短文字说明回答经事件回调交给交互层展示，保留核心代码中的输出接口；事件转发链路和终端渲染实现留到交互篇，不在循环篇展开。省略流式显示的演示需注明范围。
- 区分一次模型调用与一次 Agent 运行；不要用“程序”含糊指代已有明确名称的对象。`turns` 等计数器先说明计数对象，演示使用“模型回复 N 次”；运行正常结束不等于任务已经验证通过。
- 内部消息和事件首次出现时说明来源，不让读者误以为是通用 API 格式。源码节选保留主要调用与状态变化，用明确的省略标记移除无关参数；注释须核对函数实际职责。避免写入易过时的默认配置值。
- 模拟演示与正文使用相同的任务场景，说明按主要动作推进、跳过部分判断。中断示例若依赖工具顺序执行，要写清前提。
- 模型推理内容的可见性取决于服务返回的信息，工具执行和上下文记录由 harness 管理，不混为一谈。
- 00 的例子写清现象与预期，并标为教学示意；先解释模块用途，worktree、沙箱、事件钩子等细节留到专章。
- harness 首次出现时说明它与模型共同构成 Agent；用户视角的使用与展示称 Agent，软件职责称 harness，具体动作优先用模块名。子代理是受委派的 Agent，委派与回收由主 Agent 组织。
- 第 3 节使用静态流程图：记录任务 → 调用模型 → 记录回复 → 判断工具请求；“是”分支执行工具并记录结果，从右侧返回模型调用，“否”分支从左侧到显示回答并结束。节点只写动作，模块职责留给架构图。中断、轮数上限和模型错误用图下注明，01 再展开。流程图无需 JavaScript，文字保持可选择，并提供完整的无障碍流程说明。

## 展示与技术约定

采用 MDX，保留 Markdown 正文，组件实现与文章分离。继续使用现有 Astro 和静态部署，不增加在线模型调用或新的前端框架。

### 视觉方向：可阅读的工程手记

- 连贯的单栏正文；图示只出现在解释结构或状态的位置。
- 图示使用局部色板：暖灰画布 `#eee8dd`、浅米白 `#fffcf6`、正文 `#343137`、辅助文字 `#706971`、执行紫 `#7150a3`、结果绿 `#087c6e`；深色主题提供对应颜色。
- 系统中文无衬线字体用于正文和注释，等宽字体只用于代码、哈希等精确信息。
- 架构图用边界表达职责，紫色突出执行中心，绿色用于工具与环境反馈。
- 架构图的每个模块使用独立的闭合边框和卡片底色；运行程序外框与内部模块保持层级差异。上下文、会话、权限、扩展分别展示，分组仅说明职责，不暗示执行先后；窄屏按图示容器宽度切换纵向排列。
- 不增加自动播放动画。手机上结构图转为纵向，正文和图注不依赖悬停。
- 静态文字承担完整解释；交互只帮助观察变化，不把核心结论藏在点击或视频中。

已评估课程落地页、仪表盘和工程手记三种方向：选择工程手记，因为本系列以长文解释和源码解释为中心。用户随后选择 Josh Comeau 为布局参考，并要求全站浅色主题改用暖米色；以文末的选定方案为准。

### 分三层制作

1. 正文、伪代码、表格和静态结构图先完成；图示保持可选择、可检索的文字。
2. 对循环、压缩、分支等动态机制，按文章需要制作一个目标明确的交互组件。
3. 按需用真实日志、文件修改前后对照和录屏辅助解释；长内容提供摘要、说明或文字稿。提交记录留在内部素材笔记中查证。

00 使用三个静态 Astro 组件：`AgentArchitecture`、`TaskOverview`、`SeriesDirectory`。采用语义 HTML 与 CSS 实现响应式图示，避免把手机上的文字缩进一张过宽的图。暂无必要引入 Mermaid 运行时；后续复杂时序图可评估构建时生成 SVG。

共用色板放在 `src/components/series/series-visual.css`，选择器限定在 `.series-visual`。后续交互优先用组件中的小段脚本，需要复杂状态时再评估框架。

## 内部素材与实现依据

- imp 核对基线：`4d2548f499570392a92db573001b94d7dc3f38c0`；阅读日期 2026-09-15。
- 现有远程跟踪主线：`35e731bd22103ef5f63e47cae39c575f249ab0d2`。它不是本次联网确认的最新远程状态。
- 时间线依据 `git log --first-parent` 与具体提交，日期表示进入历史的时间，不估计开发耗时。
- 项目计划混有旧勾选项和日期；与 Git 或当前实现冲突时需核对再用。
- 旧版时间线数据仅保留作内部查证素材，00 不再渲染 `DevelopmentTimeline`。
- 架构是职责示意，不是代码导入依赖图。子代理复用循环，worktree 分离工作目录但不提供操作系统沙箱。

旧版开篇的细节素材已迁入 [后续章节素材](coding-agent-source-notes.md)，用于 01/03/05，避免重复放在 00。

## 写作与发布约定

- 统一标签 `Coding Agent`、`imp`；标题 `Coding Agent 原理与实践 01：最小 Agent 与执行循环`。
- 文章页首将系列名作为小字号独立一行，H1 只显示“序号：本篇题目”，例如 `00：为什么开发 imp`。完整标题仍保存在 frontmatter，供页面元信息、分享与列表使用；后续文章沿用统一模板，不在正文手动换行或缩小字号补救。序号与题目开头保持相连，长标题允许自然换行。
- 标题排版需检查 1600px 桌面、1280px 三栏临界宽度和 390px／320px 手机：系列名与本篇题目层次清楚，避免词语被生硬拆开、序号单独成行或标题溢出；标题改名后也要复查。
- 文章使用 `.mdx`。00 保留文件名主体 `coding-agent-00-under-the-hood`，避免改动现有预览与未来固定链接。
- 章节目录数据位于 `src/data/coding-agent-series.ts`；组件按目录数据查找已发布文章。当前章显示“本篇”，其他未发布章节显示“计划”，不生成不存在的链接。
- 专题页 `/topics/#coding-agent` 按标签收录已发布文章，并按首次发布时间排列；按目录顺序发布，修改正文只更新 `modDatetime`。
- 00、01、02 上下篇已设为 `draft: false`；首次发布时间保留各自发布时的 UTC 时间。后续文章先设为 `draft: true`，可在临时副本中设为 `false` 预览，源稿不因此上线。
- 发布前更新实际日期，检查正文、图示、目录锚点、明暗主题、手机布局、专题、标签、RSS、搜索索引与无 JavaScript 阅读。
- 不把原始日志、凭据或私人素材加入公开仓库。`draft` 只控制网站输出，不隐藏 Git 文件。

## 本轮完成范围

00 的旧版已发布。本轮修订补齐正文和目录编号，移除开发时间线、提交链接及现状清单，阅读方式统一为四步模板；文字改为具体场景和动作，保留作者动机，删去重复的抽象引导。00 使用三个静态图示组件，本轮与 01 一同发布。01 按四步结构展开，并补充衔接下一篇的简短总结：读取文件的场景、带中文注释的源码节选、模拟运行、错误与停止处理；正文已移除开发时间线、提交说明与测试报告；`AgentLoopDemo` 提供正常、工具错误重试、轮次上限、批次中断四种逐步演示。场景数据在 `src/data/agent-loop-demo.ts`，不连接模型或执行命令；禁用 JavaScript 时保留完整正文、伪代码与正常路径文字稿。01 源稿改为 `draft: false`，首次发布时间设为本次发布的实际时间。第 02 章上下篇均已审查通过并设为 `draft: false`。上篇用两次请求对照解释消息转换，下篇用 `ProviderStreamDemo` 展示正常、交错工具片段、截断和中断四种场景。

## UI 选定方向（2026-09-15）

最新选择为 [Claude Code 文档式三栏](https://code.claude.com/docs/en/worktrees)：左侧文章列表，中间正文，右侧当前文章目录。此前的 Josh Comeau 方案被这一布局取代，保留暖米色阅读主题和嵌入式交互演示。

- 页面外层最大宽度 90rem。1600px 窗口下，左右内边距各 32px，左栏 256px，右栏 264px，两处栏间距各 64px，中间正文 728px，接近参考页约 720px 的阅读宽度。
- 左侧按专题组织文章，显示当前文章；未发布章节标记为计划，没有链接。其他文章另列一组。
- 1280px 以上显示三栏；1024–1279px 保留左侧文章列表，页内目录折叠到正文上方；更窄时文章列表也折叠。
- 顶部导航保持可见，两侧导航各自随页面滚动停靠；右侧目录标记当前小节。
- 正文标题和段落、代码、图示共享中间栏，避免两侧导航挤压正文后产生横向溢出。演示在自身宽度达到 780px 时才采用两列。
- 浅色背景 #f6f1e7、文字 #343137、强调色 #7150a3、表面 #fffcf6；保留深色主题。浅色代码高亮使用 github-light，保证注释可读。
- 所有修改先在本地预览并验证，再随 00 修订和 01 正式文章一同发布。


## 第 02 章初稿核对记录（2026-09-16）

- 实现依据：imp `4d2548f499570392a92db573001b94d7dc3f38c0` 的 `src/provider/openai-completions.ts`、`shared.ts`、`anthropic.ts`、`types.ts`、`src/core/messages.ts`、`src/core/loop.ts` 和 `test/openai-completions.test.ts`；哈希仅用于素材查证，不写入文章正文。
- 主线只展开当前 Chat Completions 适配器，Anthropic 仅作字段转换对照；不展开登录、模型目录、思考配置、费用计算与终端渲染。
- 准确性：`parseSse` 是按 LF 空行拆帧的简化实现，会跳过不可解析帧；`safeParseJson` 返回错误对象，不等于已经拦截工具执行；`message_end` 不等于任务完成，当前循环也没有自动续写 `max_tokens` 回复的分支。上述限制应明确说明。
- 外部字段核对：[OpenAI 函数调用文档](https://developers.openai.com/api/docs/guides/function-calling)，重点核对 Chat Completions 的工具定义、结果关联与按 index 组装参数。
- 演示输入为预设的已解析事件，字节拆帧在正文说明；不假装是线上请求日志。无 JavaScript 时保留步骤文字稿。

### 第 02 章接入架构图

- 上篇第一节使用 `ProviderArchitecture` 展开 Agent Loop → 模型接入 → 外部服务的关系；接入层内部按请求转换、流式解析与回复组装、HTTP 通信划分职责。请求与响应使用不同方向的箭头，并标明上下篇的讲解范围。
- 厂商与协议分开描述；图中 OpenAI 使用 Chat Completions、Anthropic 使用 Messages、Z.ai 复用 Chat Completions 接入，仅表示本文涉及的路径。实现依据补充 `src/provider/zai.ts`。
- 架构图中的循环模块统一标为 `Agent Loop`，附中文 `Agent 循环`；00 总览图与 02 局部图保持一致。

## 02 上发布检查（2026-09-16）

- 上篇聚焦请求与消息转换，保留架构图、工具结果交接图、源码转换分支和两次请求对照；重试与取消仅简述原则。
- 上篇首次发布时，下篇仍是草稿，结尾暂用“待发布”文字；现已随下篇发布恢复篇间链接。
- 本次同步发布文章字数与代码行数统计，页面仅展示结果，不展示统计口径说明。

## 02 下发布检查（2026-09-16）

- 下篇审查完成，设为 `draft: false`，首次发布时间更新为本次发布的 UTC 时间；上篇保留首次发布时间，更新修改时间并恢复下篇链接。
- 正文讲解 SSE 事件解析、工具参数拼接、内部事件与消息的关系；异常处理覆盖缺少结束原因、取消与读取失败、参数解析失败，以及输出上限。
- `ProviderStreamDemo` 保留完整核心代码，并随步骤高亮；支持正常读取、两次读取交错到达、缺少结束原因和用户中断四种场景，无 JavaScript 时保留文字步骤。
- 侧栏和专题目录按发布状态自动收录 02 下；发布时核对文章、篇间链接、RSS、站点地图与搜索索引。

## 第 03 章内容范围（2026-09-16）

- 主题聚焦工具系统设计，主线为工具抽象、注册与暴露、调用校验、结果与错误协议；执行生命周期、副作用与调度约束贯穿调用过程。具体工具只作为例子，不按搜索、读取、编辑、命令的内部算法组织文章。
- 撤销此前按“调用 / 文件修改与命令执行”拆上下篇的安排。第 03 章以单篇 `coding-agent-03-tools.mdx` 发布，已通过内容审查并设为 `draft: false`；后续仅在独立机制确有必要时拆分，不重新变成内置工具介绍。
- 源码核对基线：imp `4d2548f499570392a92db573001b94d7dc3f38c0`。依据 `src/runner.ts`、`src/core/loop.ts`、`src/core/tools/types.ts`、`messages.ts`，以 read / bash 的行为辅助解释接口，并参考 `test/loop.test.ts` 与 `test/loop-hooks.test.ts`。
- 说明当前实现与一般设计的区别：Map 不会拒绝重名；通用工具接口没有统一超时设置；输出裁剪由具体工具负责；read 未声明并发；取消不等于回滚，失败不等于没有副作用。
- `ToolDefinitionMap` 以 read 为具体例子，承接 02 上 2.2 的 request.tools → body.tools 转换，分开展示工具对象、调用前的两种用途和回复中的名称如何对应本地实现。第 03 章不单设模拟运行：统一调用流程由第 2 节源码与第 3 节 read / bash 实例解释，失败路径在异常处理一节展开。
- 各机制内说明行为验证：拒绝时不得调用 execute，错误保留调用 ID，取消需要实际停止操作。权限策略、终端交互、上下文管理及完整并发调度只交代接入关系，细节留到各自章节。

- 第 03 章在统一调用机制之后增加“两个工具，怎样接入同一套系统”：节选 read / bash 的定义与执行代码，对照参数检查、结果返回和取消方式；明确标注输出处理、超时等省略细节。后续为第 4 节异常处理、第 5 节总结；异常处理按“未执行、执行失败、取消或超时”组织，分别讲拒绝原因、执行失败的具体原因、实际操作是否停止；不单独展开重试策略和幂等性，只提醒再次执行前确认已有修改；只补充前文省略的参数错误详情构造和 bash 超时代码，避免重复结果包装与中断信号传递。

- 第 03 章发布时一并更新 00 的阅读方式和系列目录：模拟运行按需使用，文章 03 保留静态工具注册图；发布检查包含文章页面、导航、RSS、站点地图与搜索索引。

## 第 04 章内容与发布（2026-09-18）

- 核心问题：任务推进时，每次模型调用怎样获得足够、相关且放得下的信息。用“修复求和函数、不改公共接口、测试仍失败”贯穿全文，所有情景数据和摘要对照均明确标为示意。
- 第 04 章以单篇 `coding-agent-04-context.mdx` 发布，已通过内容审查并设为 `draft: false`，首次发布时间更新为发布时的 UTC 时间。信息组织与历史压缩围绕每次模型调用串起，侧栏标题与正文标题保持一致。
- `ContextRequestMap` 用“字段 / 内容来源 / 常见内容”三列表格解释 system、messages、tools，明确 tools 保存本地工具对象，不展示接入层转换；`ContextCompactionCompare` 在第 3 节以 M1–M9 和摘要 S 展示同一份历史的连续变化：原消息、切分线、摘要内容、新历史；保留消息前后使用同一份数据，确保文字一致。删去独立工具补充流程和泛化信息取舍表，无交互模拟，不执行模型调用。
- 源码核对基线：imp `4d2548f499570392a92db573001b94d7dc3f38c0`。参考 system-prompt.ts、context-files.ts、compaction.ts、runner.ts、loop.ts 及 session/store.ts 的上下文重建接口。
- 区分设计原则与当前能力：未实现逐条相关性筛选；字符数估算不是准确 Token 计数，初始估算不覆盖 system 与工具定义；findCutIndex 按实际代码向前寻找 user/assistant 边界，不照抄过时的函数上方说明；主运行器自动压缩依赖会话。
- 在对应机制处简要说明摘要可能遗漏信息、摘要请求需要预留空间、失败后不替换历史；不展开超限重试路径，不宣称摘要完整、压缩必然变短或调用必然成功。会话落盘、分支恢复等细节留到第 05 章。

- 第 04 章正文结构：第 1 节说明必要性；第 2 节分为两小节：2.1 说明上下文组成与来源，合并项目约定加载源码并引出历史增长；2.2 先用一小段官方资料介绍现有 Agent 的做法，再按怎样压缩、何时触发、怎样继续调用的顺序展开 imp 实现；第 3 节“图解：一次历史压缩的过程”用一张连续图展示切分、摘要与更新，删除重复的过程表格和请求代码块；第 4 节收尾。不单设异常处理一节：摘要可能遗漏信息、压缩调用需要预留空间、失败时保留原消息，分别并入第 2 节对应机制；删去故障处置建议、重试路径及改进方案的展开。API 字段组织与 imp 内部接口分开说明，不把 system/messages/tools 当作所有模型服务统一采用的三个顶层字段。

- 第 04 章 2.2 的方案概述依据 Claude Code 工作原理文档、Gemini CLI 的 /compress 命令说明及 OpenAI Compaction 文档（2026-09-18 核对）；分别说明按需加载与独立子代理上下文、旧工具输出清理、摘要和非明文压缩内容，不把所有产品归为同一种摘要算法。

- 第 04 章第 3 节用 9 条消息、cut = 7 的教学示例展示数组变化：前 7 条生成摘要，最后一组测试调用与结果保留，下一次 messages 为摘要 S 加 M8、M9 两条近期消息；明确这些条数不代表压缩阈值或固定压缩比例，摘要没有读取近期测试结果。


## 第 05 章内容与发布（2026-09-19）

- 核心问题：Agent 怎样保存任务进行中的记录，让用户关闭后重新打开，仍然能接着工作？
- 单篇发布，`draft: false`，发布时间为 2026-09-19；正文与侧栏均为“会话与持久化：保存记录，恢复任务”。保存、恢复是主线，暂不拆篇；2026-09-19 移除会话分支一节，分支功能另行讨论。
- 结构：使用场景 → 消息写入与 JSONL → 恢复压缩后的历史 → 异常退出 → 衔接交互篇。不重复 04 的摘要生成与压缩时机。
- 源码核对基线：imp `4d2548f499570392a92db573001b94d7dc3f38c0`；重点核对 `runner.ts`、`core/session/store.ts`、`manager.ts`、`core/loop.ts` 与 `repl/replay.ts`。节选按函数分开，标明省略内容。
- `SessionRecoveryDiagram.astro` 仅提供恢复过程的静态图，无前端依赖与脚本；重用系列主题色，窄屏纵向排列。示例消息和标识均为教学示意。
- 恢复视图为摘要 + retainedTail + 后续消息，原始消息仍保留；消息回放不执行工具；不在本篇展开分支或文件回滚。
- 异常说明依照实际实现：末行解析失败时读取容错，不会自动修复文件尾部；中间损坏及断裂父链报错。退出钩子补缺失工具结果，不能覆盖断电或立即终止；没有结果不等于没有执行，当前退出原因文案不能作为实际执行状态证明。
- 评估独立审查：本次仅文章、静态图与导航文案，无 Agent 运行逻辑变更；采用对照源码自审、构建及桌面/手机预览检查。

- 第 05 篇 2.2 按“保存内容 → 关键类型 → 连贯示例”展开，只介绍 session、message、compaction；message 展示用户任务、模型工具调用和工具结果，明确记录 ID 与工具调用 ID 的区别。position 等分支相关类型暂不展开。JSON 示例展开排版，注明实际 JSONL 每条占一行，以及省略的非关键字段。

- 第 05 篇删去原第 3 节“想从之前换个方案，怎样保留两条历史？”及分支图，异常处理、收尾改为第 3、4 节；同步清理摘要、开篇、字段说明和结尾中的分支引导。2.3 只说明 getBranch 取得当前对话路径，保留真实恢复逻辑。
